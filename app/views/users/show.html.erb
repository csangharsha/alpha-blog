<h1 align="center">Welcome to <%=@user.username%>'s profile</h1>

<div class="row mt-4">
  <div class="col-md-4 offset-md-4 d-flex justify-content-center">
      <%= gravatar_for @user, size: 250 %>
  </div>
</div>

<h4 align="center" class="mt-4"><%= @user.username %>'s Articles</h4>

<%= render 'articles/article', obj: @user.articles %>
