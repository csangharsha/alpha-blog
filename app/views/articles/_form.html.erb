<%= render "shared/error", obj: @article %>

<div class="col-sm-12">

  <%=form_for @article, data: {turbo: false} do |f|%>

    <div class="row mb-3 offset-sm-3">
      <%=f.label :title, class: "col-sm-2 col-form-label" %>
      <div class="col-sm-6">
        <%=f.text_field :title, class: "form-control", placeholder: "Title of Article" %>
      </div>
    </div>

    <div class="row mb-3 offset-sm-3">
      <%=f.label :description, class: "col-sm-2 col-form-label" %>
      <div class="col-sm-6">
        <%=f.text_area :description, class: "form-control", rows: 8 %>
      </div>
    </div>

    <div class="row mb-3 offset-sm-3">
      <div class="col-sm-9">
        <%= f.collection_check_boxes :category_ids, Category.all, :id, :name do |cb| %>
          <% cb.label(class: "checkbox-inline form-check form-check-inline form-check-label") {cb.check_box(class: "form-check-input") + cb.text} %>
        <% end %>
      </div>
    </div>

    <div class="col-sm-6 offset-sm-3">
      <%= link_to 'Back to article listing', articles_path, class: 'btn btn-outline-secondary' %>
      <%=f.submit class: "btn btn-primary float-end" %>
    </div>

  <% end %>

</div>